<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="id.kodesumsi.telkompengmas">

    <application
        android:allowBackup="true"
        android:name=".base.BaseApplication"
        android:icon="@drawable/telkom_university_logo"
        android:label="@string/app_name"
        android:roundIcon="@drawable/telkom_university_logo"
        android:supportsRtl="true"
        android:theme="@style/Theme.TelkomPengmas">
        <meta-data
            android:name="com.google.android.gms.version"
            android:value="@integer/google_play_services_version" />
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/MAPS_KEY" />
        <activity
            android:name=".ui.onboarding.OnboardingActivity"
            android:exported="false" />
        <activity
            android:name=".ui.main.MainActivity"
            android:exported="false" />
        <activity
            android:name=".ui.forms.TambahDataAnakActivity"
            android:exported="false" />
        <activity
            android:name=".ui.detail.ChildDetailActivity"
            android:exported="false" />
        <activity
            android:name=".ui.auth.AuthActivity"
            android:exported="false" />
        <activity
            android:name=".ui.splashscreen.SplashscreenActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>